<h1>{{page.name}}</h1>

{{#each cont in conts}}
  <div {{action "edit" target="cont"}} class="cont" {{bindAttr data-type="cont.type"}}>
    {{#if cont.isEditable}}
      {{textarea value=cont.cont action="saved_cont" target="cont"}}
      <button {{action "saved_cont" target="cont"}}>save</button>
    {{else}}
      {{cont.cont_string}}
    {{/if}}
  </div>
{{/each}}

<a {{action "add"}} href='location.void(0)'>[+ add content]</a>