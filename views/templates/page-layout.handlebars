<header>
  <h1><a href="/">{{name}}</a></h1>
</header>

<nav>
  {{#each page in pages }}
    <a {{bindAttr href="page.url"}}>{{page.name}}</a>
  {{/each}}
</nav>

<section class="content">
  {{yield}}
</section>
